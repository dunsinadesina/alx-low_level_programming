#include "main.h"
/**
 * _abs - This is for finding absolute value of a number
 * @a: This is a parameter
 * Return: This returns the absolute value of a number
 */
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);
}
